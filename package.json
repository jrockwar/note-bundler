{
  "name": "note-bundler",
  "version": "0.0.6",
  "description": "Obsidian plugin to export bundles of notes as a single markdown file for LLM/AI context.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs --production",
    "lint": "echo \"No lint configured yet\""
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "esbuild": "^0.19.12",
    "obsidian": "^1.5.8",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  }
}
